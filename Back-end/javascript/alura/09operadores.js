// || é "ou", true, condição seja válida
//&& Operador “e”, todas as condições forem válidas
//!= e !== Operadores “não igual”, “estritamente não igual”, comparação, da mesma forma que == e === retornam true ou false.
// doc de operadores é util

// == comparação implicita (só ve o valor)
// comparador explicito(procura tudo, inclusive valores e tipos)

// pouco uso do dois iguais e fazer a conversão com o: Number() e o String()

const num = 7;
const stri = "7";
console.log(num == stri);
console.log(num === stri);

console.log(typeof num);
console.log(typeof stri);

